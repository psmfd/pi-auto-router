{
  "name": "@psmfd/pi-auto-router",
  "version": "0.11.0",
  "description": "Pi extension: per-prompt model selection via a cheap classifier (before_agent_start -> pi.setModel)",
  "type": "module",
  "license": "MIT",
  "repository": "github:psmfd/pi-auto-router",
  "keywords": [
    "pi",
    "pi-extension"
  ],
  "pi": {
    "extensions": [
      "./index.ts"
    ]
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "tsx --test test/*.test.ts"
  },
  "dependencies": {
    "@earendil-works/pi-ai": "0.81.1"
  },
  "peerDependencies": {
    "@earendil-works/pi-coding-agent": ">=0.75"
  },
  "devDependencies": {
    "@earendil-works/pi-coding-agent": "0.81.1",
    "@types/node": "^26.1.1",
    "tsx": "^4.19.2",
    "typescript": "^6.0.3"
  }
}
